<Page x:Class="Centraly.Desktop.Views.SuppliersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="الموردين"
      FlowDirection="RightToLeft"
      Background="{StaticResource PageBackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <ui:Button DockPanel.Dock="Right" Content="+ إضافة مورد" Appearance="Primary" Width="130" Click="OnAddClick" Margin="8,0,0,0" />
            <ui:Button DockPanel.Dock="Right" Content="تعديل" Width="90" Click="OnEditClick" Margin="8,0,0,0" />
            <ui:Button DockPanel.Dock="Right" Content="حذف" Width="90" Click="OnDeleteClick" />
        </DockPanel>

        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="8" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <DataGrid x:Name="SuppliersGrid" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الاسم" Binding="{Binding Name}" Width="*" />
                    <DataGridTextColumn Header="النوع" Binding="{Binding Type}" Width="120" />
                    <DataGridTextColumn Header="الهاتف" Binding="{Binding Phone}" Width="140" />
                    <DataGridTextColumn Header="الرصيد المستحق" Binding="{Binding DebtBalance}" Width="120" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
