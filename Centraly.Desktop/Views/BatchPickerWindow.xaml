<ui:FluentWindow x:Class="Centraly.Desktop.Views.BatchPickerWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 Title="اختيار الشركة والسعر"
                 Width="520" Height="440"
                 WindowStartupLocation="CenterOwner"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 FlowDirection="RightToLeft"
                 Background="{StaticResource PageBackgroundBrush}">
    <Grid Margin="0,44,0,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="ProductNameText" Grid.Row="0" FontSize="14" Foreground="{StaticResource TextMutedBrush}"
                   Margin="20,0,20,12" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="BatchesList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Margin="20,0,20,0" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding SupplierLabel}" FontWeight="Bold" />
                                    <TextBlock Text="{Binding DateLabel}" Foreground="{StaticResource TextMutedBrush}" FontSize="12" Margin="0,4,0,0" />
                                </StackPanel>

                                <ui:Button Grid.Column="1" Content="{Binding WholesaleLabel}" Width="90" Margin="0,0,8,0"
                                           Click="OnWholesaleClick" Tag="{Binding}" />
                                <ui:Button Grid.Column="2" Content="{Binding RetailLabel}" Width="90"
                                           Background="{StaticResource PosAccentBrush}" Foreground="White"
                                           Click="OnRetailClick" Tag="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
